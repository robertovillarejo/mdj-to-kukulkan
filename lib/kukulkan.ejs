<% element.forEach(function(entity) { %>
entity <%= entity.name %> { <% entity.properties.forEach(function(attr, index1, array1) { %>
        <%=attr.name%> : <%=attr.type%><%if (attr.required){%> required<%}%><%if (index1 < array1.length - 1 || entity.relationships.length !== 0){%>,<%}%><% }); entity.relationships.forEach(function(rel, index2, array2) { %>
        <%=rel.type%> <%if (rel.attributeNameInTarget){%>(<%= rel.attributeNameInTarget%>) <%}%><%=rel.attributeName%> : <%=rel.targetEntity%><%if (rel.required){%> required<%}%><%if (index2 < array2.length - 1){%>,<%}%><% }); %>
}
<% }); %>