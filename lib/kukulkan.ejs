<% element.forEach(entity => { %>
entity <%= entity.name %> { <% entity.properties.forEach(attr => { %>
        <%=attr.name%> : <%=attr.type%>,<% }); entity.relationships.forEach(rel => { %>
        <%=rel.type%> <%if (rel.attributeNameInTarget){%>(<%= rel.attributeNameInTarget%>) <%}%><%=rel.attributeName%> : <%=rel.targetEntity%><%if (rel.required){%> required<%}%>,
    <% }); %>
}
<% }); %>