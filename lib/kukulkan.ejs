<% element.forEach(entity => { %>
entity <%= entity.name %> { <% entity.properties.forEach(attr => { %>
        <%=attr.nam%> : <%=attr.type%>,<% }); entity.relationships.forEach(rel => { %>
        <%=rel.type%> <%=rel.name%> : <%=rel.targetEntity%>,
    <% }); %>
}
<% }); %>